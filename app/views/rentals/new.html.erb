Movie: <%= @movie.title %> (<%= link_to "back", new_movie_path %>)

<hr/>

<%= form_for @rental, :url => { :action => :create, :id => @movie.id} do |f| %>

	Borrowed on: <%= f.date_field :borrowed_on , as: :date %> <br />
	Returned on: <%= f.date_field :returned_on %> <br />
	<%= f.submit %>

<% end %>

<br><br>
<hr/>
<br>
List of all date: <br><br>
<%if @movie.rentals.exists? %>
	<% for item in @movie.rentals %>
	<%= item.borrowed_on %>, <%= item.returned_on %> <br>
<% end %>

<% else %>
no rentals yet
<% end %>